# Operational CLI Blueprint

This blueprint describes the command-line entry point and execution playbook implemented on Day 22 of the engineering sprint.

## CLI Options & Syntax
```bash
python cli.py --dir <PATH_TO_PROJECT> --id <SUBMISSION_ID> --output <json|markdown>